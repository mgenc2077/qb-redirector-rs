[package]
name = "qb-redirector-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["blocking", "multipart", "cookies", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
dirs = "6"
